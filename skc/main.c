#include <stdio.h>

int main(int argc, char * argv[])
{
	if (argc < 2)
	{
		printf("The path to the input file is missing.\n");
		return -1;
	}

	printf("Simpified Kotlin Compiler Project.\n");
	return 0;
}
